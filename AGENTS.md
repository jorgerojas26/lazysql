## Agent skills

### Issue tracker

Issues (called "beans") live in the repo's beans tracker and are managed with the `beans` CLI. Run `beans prime`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` at the repo root plus ADRs in `docs/adr/`. See `docs/agents/domain.md`.